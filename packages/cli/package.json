{
  "name": "@watchbridge/cli",
  "version": "0.1.0",
  "license": "0BSD",
  "type": "module",
  "bin": {
    "watchbridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@watchbridge/core": "workspace:*",
    "@watchbridge/connectors": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.10",
    "@types/node": "26.1.1",
    "typescript": "5.7.3",
    "vitest": "4.1.10"
  }
}
