{
  "name": "@watchbridge/core",
  "version": "0.1.0",
  "license": "0BSD",
  "type": "module",
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "package.json"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "5.7.3",
    "vitest": "4.1.11"
  }
}
