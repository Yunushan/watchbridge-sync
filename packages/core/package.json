{
  "name": "@watchbridge/core",
  "version": "0.1.0",
  "license": "0BSD",
  "type": "module",
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "package.json"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
