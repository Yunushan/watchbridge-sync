{
  "name": "watchbridge-sync",
  "private": true,
  "version": "0.1.0",
  "description": "Free/open-source media data export, import, and sync workspace for IMDb, Letterboxd, TMDb, Trakt, Simkl, TV trackers and more.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "corepack pnpm --parallel --filter @watchbridge/api --filter @watchbridge/web dev",
    "build": "corepack pnpm -r --sort build",
    "test": "corepack pnpm -r test",
    "lint": "corepack pnpm build && corepack pnpm -r --sort lint",
    "format": "prettier -w .",
    "check": "corepack pnpm lint && corepack pnpm test && corepack pnpm build"
  },
  "devDependencies": {
    "@types/node": "latest",
    "prettier": "latest",
    "typescript": "latest",
    "vitest": "latest"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "pnpm": {
    "overrides": {
      "typescript": "5.7.3"
    }
  }
}
